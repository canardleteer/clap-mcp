name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: CI (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run tests (all features)
        run: cargo test --all-features

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Security audit
        if: matrix.os == 'ubuntu-latest'
        uses: rustsec/audit-check@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and run examples (release validation)
        shell: bash
        run: |
          cargo build -p clap-mcp-examples --all-features
          for bin in subcommands subcommands_http structured tracing_bridge log_bridge async_sleep async_sleep_shared async_embedder_serve struct_subcommand optional_commands_and_args result_output subprocess_exit_handling panic_catch_opt_in client task_tools_dedicated task_tools_shared task_augmented_client task_serial_probe_dedicated task_serial_probe_shared task_parallel_probe_dedicated task_parallel_probe_shared task_panic_catch; do
            cargo run -p clap-mcp-examples --all-features --bin "$bin" -- --help
          done

      - name: Check rustdoc (no warnings)
        env:
          RUSTDOCFLAGS: "-D warnings"
        run: cargo doc --no-deps -p clap-mcp --all-features
